# AWS-SQS

### Install dependencies

``` pip install boto3 ``` or ``` conda install boto3 ```

### Run Publisher

``` python src/SQSPublisher.py ```

### Run Consumer 

``` python src/SQSPublisher.py ```
